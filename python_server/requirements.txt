# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.12.0
cryptography>=43.0.0
httpx>=0.27.0
python-multipart>=0.0.9

# === AI Service Dependencies ===

# PDF Parsing
pdfplumber>=0.11.0
pandas>=2.2.0

# Image Processing & Wireframe Detection
opencv-python>=4.9.0
numpy>=1.26.0
Pillow>=10.3.0

# Deep Learning (optional — for GPU-accelerated generation)
# torch>=2.3.0
# torchvision>=0.18.0
# diffusers>=0.27.0
# transformers>=4.40.0
# accelerate>=0.30.0

# PDF rasterization (fallback for PDF wireframe extraction)
# pdf2image>=1.17.0

# HAWP wireframe parsing (optional)
# hawp>=0.1.0  # Install from: git+https://github.com/cherubicXN/hawp.git

# === Voice-to-Text ===
# OpenAI Whisper for server-side audio transcription (fallback when Web Speech API is unavailable)
# Requires ~1.5GB VRAM for 'base' model, ~4GB for 'small'
openai-whisper>=20240930
# Required by whisper for audio file handling
soundfile>=0.12.0
